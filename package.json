{
  "name": "root",
  "private": true,
  "scripts": {
    "compile": "lerna run compile",
    "test-coverage": "lerna run test-coverage --stream",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
