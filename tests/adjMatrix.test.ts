import {
    AdjacencyMatrix,
    AdjacencyMatrixParams,
    //Token,
} from '../src/graph';

describe('AdjacencyMatrix factory methods', () => {
    it('creates a matrix from a generator of tokens', () => {
        expect(true);
    })
})
